using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Color1 : MonoBehaviour
{
    /*Color saveColor;
    // Start is called before the first frame update
    void OnMouseEnter()
    {
        if (GetComponent<Renderer>().material.color != Color.red)
        {
            saveColor = GetComponent<Renderer>().material.color;
        }
        GetComponent<Renderer>().material.color = new Color(131 / 255.0f, 164 / 255.0f, 181 / 255.0f);
    }
    void OnMouseExit()
    {
        GetComponent<Renderer>().material.color = saveColor;
    }*/
}
